import React from "react";
import RadialTimer from "./RadialTimer";

export default {
  title: "Component/RadialTimer",
  component: RadialTimer,
};

const Template = (args) => <RadialTimer {...args} />;

export const Default = Template.bind({});
